# 개발 기록 - 2026-08-16

## 요약

- **AGENTS.md 자동 실행 절차 추가**: 세션 시작 시 auto-resume 플러그인 확인, 토큰 5% 잔량 시 작업 기록, 세션 시작 지점 점검 규칙을 AGENTS.md 섹션 0에 기록 (커밋 `8741caf`)
- **RVC 백엔드 검증 (사용자 선택 "RVC 백엔드 검증부터")**: `backend/services/vc/pipeline.py`가 vendored RVC 엔진(`backend/rvc_engine`)과 실제로 맞물리는지 전수 감사 → 치명 버그 5건 발견·수정

## 진행 사항

### RVC 파이프라인 감사 (vs vendored 엔진)

- [x] 엔진 스크립트(`preprocess.py`, `extract_f0.py`, `extract_hubert_feature.py`, `train.py`, `train_index.py`, `infer/cli.py`, `process_ckpt.py`)의 실제 `sys.argv`/argparse 인자 규격 확인
- [x] 파이프라인 호출부(`_run_vc_process`)와 인자 개수·순서·경로 매칭 여부 전수 비교

### 발견/수정된 치명 버그 (5건)

1. **`configs/v2/40k.json` 부재** — v2는 32k/48k만 존재, 40k 학습 시 `make_config()` 실패. → `configs/v1/40k.json`을 복사해 생성 (sr=40000, hop=400, upsample=[10,10,2,2] 값 검증 완료)
2. **config 복사 경로 오류** — `configs/v2/{sample_rate}.json`(`40000.json`)을 찾았으나 엔진 명명은 `40k.json`. → `sr_label = f"{sample_rate // 1000}k"` 도입
3. **`-sr` 인자 오류** — `str(sample_rate)`("40000")를 넘겼으나 엔진 `models.py:555`는 `sr2sr[sr]` 딕셔너리 키("40k"/"48k") 사용. → `sr_label`("40k")로 전달
4. **`filelist.txt` 미생성** — `preprocess.py`는 파일리스트를 만들지 않지만 `train/utils.py:400`이 `hps.data.training_files = "%s/filelist.txt"`를 요구 → `_write_filelist()` 헬퍼 추가 (포맷: `<gt_wav>|<3_feature768>|<2a_f0>|<2b-f0nsf>|<spk_id>`)
   - 주의: hubert는 `{stem}.npy`, f0/f0nsf는 `{name}.npy`(확장자 포함)로 명명 불일치 → 각각 맞게 경로 생성
5. **`_finalize_model` 산출물 오류** — raw `G_*.pth`(학습 체크포인트, `{"model":...}`)를 복사했으나 추론은 `savee()` 산출물(`opt["weight"]`/`opt["config"]`/`opt["sr"]`) 필요 → `WEIGHTS_DIR/{model_id}.pth`(savee 출력) 우선, 없으면 G_*.pth 폴백. 인덱스도 `added_IVF*.index`/`INDICES_DIR/*{model_id}*.index` glob으로 수정

### 추가 수정

- [x] **48k pretrain 지원**: 다운로더(`download_rvc_assets.py`)에 `f0G48k.pth`/`f0D48k.pth` 추가, `_preflight_assets(sample_rate)`가 SR별 pretrain 검사, `run_train_job`이 SR별 pretrain 선택
- [x] **convert f0_method 클램프**: 라우트는 `harvest` 허용하나 `infer/cli.py`는 `pm`/`rmvpe`만 지원 → `harvest` 등은 `rmvpe`로 강제
- [x] **`assets/weights/` 생성**: `savee()`가 상대경로로 쓰는 디렉토리 (torch.save는 부모 디렉토리 미생성)

### 검증

- [x] `python3 -m py_compile` 전체 통과 (backend/services/vc/*, routes/vc.py, models.py, scripts/*)
- [x] Windows Python 3.12에서 `backend.services.vc.pipeline` import 성공 (`SUPPORTED_SAMPLE_RATES={40000,48000}`, 경로 정상)
- [x] `_write_filelist` 단위 테스트: 파일리스트 생성 + 누락 feature 시 RuntimeError 가드 통과
- [x] `train.py` argparse dry-run: `-e/-sr/-f0/-bs/-g/-te/-se/-pg/-pd/-l/-c/-sw/-v` 파싱 확인
- [x] `_preflight_assets(40000)` 통과, `_preflight_assets(48000)`은 48k pretrain 미설치로 정상 raise
- [x] `infer/cli.py` 인자 규격(`--model/--input/--output/--pitch/--f0-method/--protect/--format/--index-rate`)과 convert 호출부 일치 확인

## 다음 할 일

1. **커밋**: RVC 파이프라인 수정 사항 커밋 (fix: RVC 훈련 파이프라인 config/-sr/filelist/산출물 버그 수정)
2. **RVC 전용 venv 생성**: `VOICEBOX_RVC_PYTHON` 또는 `backend/rvc_engine/.venv`에 의존성 설치 (현재는 `sys.executable` 폴백이라 Windows Python 3.12 사용 중)
3. **48k pretrain 다운로드**: `python -m backend.scripts.download_rvc_assets` 재실행 (f0G48k/f0D48k 추가분)
4. **실훈련 E2E 테스트**: GPU 환경에서 실제 40k 훈련 한 사이클 돌려 파이프라인 전체 검증 (가능하면 짧은 epoch)
5. **실추론 테스트**: 훈련 산출물로 convert 한 번 수행

## 커밋 기록

- `8741caf` docs: 세션 자동 시작 절차 추가 (auto-resume 플러그인 확인, 토큰 5% 자동 기록)

---

## 후속: RVC E2E 전체 파이프라인 검증 완료 (GPU 실동작)

로컬 GPU(RTX 4070 Ti SUPER 16GB, CUDA 13.1)를 활용해 훈련→변환 전체 E2E를 API로 실행·검증.

### 환경 구성

- [x] **RVC 전용 venv 생성** `backend/rvc_engine/.venv` (Windows Python 3.12):
  - `torch 2.13.0+cu126` + torchaudio (CUDA GPU 인식 확인)
  - numpy/scipy/librosa/soundfile/**praat-parselmouth**(주의: `parselmouth`는 가짜 패키지 — `praat-parselmouth` 사용) /transformers/faiss-cpu/scikit-learn/resampy/pyworld
  - `ffmpeg-python` + `av`(PyAV), `tensorboard`, `matplotlib`(훈련 시각화)
- [x] `get_rvc_python()`이 `.venv` 자동 감지 → `engine_ready: true, can_train: true` 확인
- [x] 48k pretrain(f0G48k/f0D48k.pth) 다운로드, `_preflight_assets(40000/48000)` 둘 다 통과
- [x] **ffmpeg/ffprobe 설치**: 시스템에 없어서 `backend/rvc_engine/bin/`에 v4.4.1 portable 배치. `download_rvc_assets.py`에 `download_ffmpeg()` 단계 추가. (주의: gyan.dev 9.0 빌드는 실행 안 되고 4.4.1이 정상 동작)

### 발견·수정한 버그 (E2E로 검출)

1. **`SessionLocal` 초기화 순서 버그** (`database/session.py`): 모듈 import 시점에 `None`으로 고정돼 vc 라우트/서비스에서 `SessionLocal()`이 TypeError. → `_SessionFactory` lazy callable로 변경 (init_db() 이후 실제 sessionmaker 해석)
2. **VC 큐 워커 coroutine 처리 버그** (`services/vc/jobs.py`): 라우트가 coroutine 인스턴스를 전달하는데 워커가 `await job.coro()` 호출. → `iscoroutine()` 분기로 수정
3. **`delete_vc_model` 상대 import 버그** (`routes/vc.py:372`): `__import__("..services.vc.pipeline")`가 실패 → 최상단에서 `LOGS_DIR` import로 변경
4. **엔진 스크립트 circular import**: `python train/preprocess.py` 실행 시 sys.path[0]이 `train/`이 되어 `train` 패키지가 `train/train.py`에 가려짐. → 파이프라인에서 `python -m train.preprocess` / `-m train.dataset.extract_f0` / `-m train.dataset.extract_hubert_feature` / `-m train.train` / `-m train.train_index` / `-m infer.cli`로 실행 (cwd=engine_dir)
5. **ffmpeg PATH**: `infer/audio.py`가 ffmpeg CLI를 쓰므로 파이프라인이 `engine_dir/bin`을 PATH 앞에 주입 (`_add_engine_bin_to_path`)
6. **tensorboard/matplotlib 미설치**: 훈련 스크립트가 요구 → venv에 추가 설치

### E2E 검증 결과

- [x] **업로드** 3개 wav (각 6초) → file_id 반환
- [x] **훈련** (40k, 3 epoch, batch 2): preprocessing(6 chunk) → F0(6/6) → HuBERT(6/6, GPU 768-dim) → 학습(손실값 정상 계산) → 인덱스 → `_finalize_model` → **status: done, 모델 ready**
- [x] 산출물: `data/vc/models/e2e_test_model/`에 `.pth`(55MB) + `.index` 생성
- [x] **변환**: 훈련 모델로 test_voice1 변환 → 40kHz/6초/모노 PCM wav 정상 출력
- [x] `/api/vc/models` 조회에서 `status: ready`, `/vc/status`에서 `engine_ready: true`

### 다음 할 일

1. **커밋**: RVC E2E 수정(session/jobs/route/pipeline 모듈실행/ffmpeg 다운로더) + devlog
2. **테스트용 자산 정리**: `data/vc/`의 E2E 테스트 산출물(e2e_test_model, uploads, results) 삭제 여부 결정
3. **`bin/` 커밋 정책**: ffmpeg.exe(100MB+)/ffprobe.exe가 `.gitignore`의 `*.exe`에 걸림. 재현은 `download_rvc_assets.py`로 가능하나, 배포 시 자동 설치 경로 확인 필요
4. **웹 UI 연동**: 프론트엔드 VoiceProfiles에서 VC 훈련/변환 API 연결 확인
5. **PyInstaller 패키징**: `voicebox-server.spec`에 RVC 엔진/venv/자산 포함 여부 점검