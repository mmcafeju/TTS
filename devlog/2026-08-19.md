# 2026-08-19 — 도움말/튜토리얼 미표시 문제 해결

## 상황
사용자가 "튜토리얼/도움말 기능이 안 보인다"고 보고 → 브라우저 실행해 확인.

## 원인
- 코드는 정상 커밋 상태(도움말/튜토리얼 시스템 구현 완료).
- **vite dev 서버(5173)가 꺼져 있어** 열려 있던 브라우저 탭들이
  `ERR_CONNECTION_REFUSED` 상태로 연결 오류 화면만 표시 중이었음.

## 조치
1. vite dev 서버 재기동: `web` workspace, 포트 5173 (Windows cmd, `web/vite.log`)
2. CDP(9222)로 상태 확인 → 연결 거부 확인
3. `scripts/help_browser_test.mjs` 전체 E2E 실행 → 통과
   (사이드바 아이콘, /help 14개 메뉴, 튜토리얼 1~5, 클로닝 규격 박스, 장문 힌트, 콘솔 에러 0)
4. 열려 있던 17개 페이지 탭 전부 reload → 정상 표시 확인

## 메모
- dev 서버 재시작 필요 시: `cd /d D:\TTS\web && npm run dev` (Windows)
- 백엔드(17493)는 이번 작업과 무관(도움말은 순수 프론트).

## 다음 할 일
- (기존 devlog 미완료 목록 유지) RVC 엔진 배포/frozen 검증 등

## 도움말/튜토리얼 미표시 — 근본 원인 수정 (백엔드 서빙 빌드 갱신)

- 사용자가 Edge(127.0.0.1:17493)에서 도움말/튜토리얼 아이콘을 못 찾음 보고.
- 원인: 백엔드(17493)가 서빙하는 `frontend/` 빌드가 도움말 기능 커밋(8/18 15:09)
  이전(8/18 14:27) 구버전. JS에 helpData/HelpTab/circle-help 없음.
- 조치:
  1. `web`에서 vite 빌드 (Windows node: `node ..\node_modules\vite\bin\vite.js build`)
     → `web/dist` 산출.
  2. `frontend/`에 복사 (rm assets/index.html + cp web/dist/*).
  3. CDP 크롬 + Playwright(msedge, playwright-core)로 17493 접속 검증:
     사이드바 9개 링크 중 8번째 `도움말`(/help, y=524) 정상. 스크린샷 저장.
  4. 캐시된 Edge 창(10072, InPrivate) 종료 후 `/help` 새 창(14260)으로 재오픈
     → UIA로 좌측 메뉴 14개(개요+기능8+튜토리얼5) 표시 확인.
- 검증 도구: playwright-core 설치 (C:\Temp\opencode), UIAutomation.
