# 2026-08-17 — 세션 기록 (RVC VC 기능 완성)

## 세션 요약

이전 세션에서 이어받은 지점: RVC 백엔드 E2E(업로드→훈련→변환)가 로컬 GPU에서 검증 완료된 상태.
이번 세션에서 **프론트엔드 VC 연동 UI를 구현·커밋**하여 백엔드+프론트엔드 양쪽 모두 완성됨.

## 완료된 것

1. **RVC 백엔드 E2E 검증** (이전 세션 이어받음, `16690cf` 커밋)
   - 로컬 GPU(RTX 4070 Ti SUPER 16GB, CUDA 13.1)로 훈련→변환 전부 성공
   - 훈련: preprocessing(6 chunk) → F0(6/6) → HuBERT(6/6, GPU 768-dim) → 학습 → 인덱스 → finalize → `status: done`
   - 변환: 결과 40kHz/6초/모노 PCM wav 정상
   - 수정된 버그 6종: SessionLocal lazy callable, jobs coroutine 분기, routes/vc 상대import, 엔진 `-m` 모듈 실행, ffmpeg bin PATH 주입(`_add_engine_bin_to_path`), tensorboard/matplotlib 설치

2. **프론트엔드 VC 연동 UI** (이번 세션, `1c896de` 커밋)
   - `app/src/lib/api/types.ts`: VC 타입 6종 (VCUploadResponse/VCTrainRequest/VCConvertRequest/VCModelResponse/VCJobResponse/VCStatus)
   - `app/src/lib/api/client.ts`: `/vc/*` 메서드 9종 (status/upload/train/convert/models list·rename·delete/jobs/results URL)
   - `app/src/lib/hooks/useVC.ts`: useVCStatus(15s 폴링)/useVCModels/useVCJobs/useVCJob(완료시 폴링중단)/useUploadVC/useTrainVC/useConvertVC/useDeleteVCModel
   - `app/src/components/VCTab/VCTab.tsx`: 훈련 패널 + 변환 패널(진행바·결과링크) + 모델/잡 테이블 + 엔진 상태 뱃지
   - `router.tsx`/`Sidebar.tsx`: `/vc` 경로 + Radio 아이콘 등록
   - i18n: ko 완역 + 8개 로케일 en 폴백
   - 검증: typecheck 통과 / Windows node로 build 성공(3231 modules) / VCTab·useVC biome 클린

## 미완료 (다음 세션 할 일)

1. **UI 실동작 테스트**: 브라우저에서 dev 서버로 VC 탭의 훈련/변환 클릭 흐름 확인
   - 서버 실행 중 (Windows Python 3.12, port 17493, 로그 `/tmp/opencode/voicebox_server.log`)
   - `e2e_test_model` ready 상태 존재 → 변환 UI 바로 테스트 가능
2. **테스트 자산 정리**: `data/vc/`의 E2E 테스트 산출물(e2e_test_model/업로드/결과) 삭제 여부 결정 (gitignore 대상이라 커밋엔 영향 없음)
3. **PyInstaller 패키징 설계**: `backend/voicebox-server.spec`에 RVC 엔진 포함 방안
   - RVC는 **별도 venv 서브프로세스**로 실행 (`backend/rvc_engine/.venv`) → 번들 대상 아님
   - `get_rvc_engine_dir()`이 `Path(__file__).parent / "rvc_engine"` 소스 상대경로 → **frozen 모드 미지원**
   - `VOICEBOX_RVC_PYTHON` 환경변수로 인터프리터 오버라이드 가능 (gpu.py 확인)

## 실행 환경 메모 (다음 세션 필수)

- 서버 재시작: `tasklist.exe | grep python.exe` → `taskkill.exe /PID $PID /F` → `setsid nohup /mnt/c/Users/joo1/AppData/Local/Programs/Python/Python312/python.exe -m uvicorn backend.app:app --host 127.0.0.1 --port 17493 > /tmp/opencode/voicebox_server.log 2>&1 < /dev/null & disown`
- API 테스트: WSL curl은 Windows 127.0.0.1 접근 불가 → `/mnt/c/Windows/System32/curl.exe` 사용
- RVC venv: `backend/rvc_engine/.venv` (Windows Python 3.12, torch 2.13.0+cu126)
- ffmpeg/ffprobe: `backend/rvc_engine/bin/` (v4.4.1, 다운로더 `download_rvc_assets.py`에 포함됨)
- 프론트 빌드: WSL은 rollup 네이티브 없음 → Windows node로 `cmd.exe /c "cd /d D:\TTS\app && npm run build"` 또는 npm.cmd
- 엔진 스크립트: 반드시 `python -m train.preprocess` 형식 (sys.path[0] shadowing 방지)
- `parselmouth`는 가짜 패키지 → `praat-parselmouth` 사용

## 커밋 기록 (이번 세션까지)

- `1c896de` feat: 프론트엔드 VC(음성 변환) 탭 구현 (16 files, +1383)
- `16690cf` fix: RVC E2E 파이프라인 검증 중 발견한 오류 수정 (6 files, +849)

## 컨텍스트 참고 (토큰 압축 대비)

전체 작업 히스토리는 `devlog/2026-08-16.md`와 본 파일에 기록됨. 다음 세션은 AGENTS.md 0.3에 따라 이 파일의 "미완료"부터 시작하면 됨. RVC 백엔드/프론트 구현 상세는 이 두 devlog + 커밋 diff로 충분.