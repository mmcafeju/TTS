# 개발 기록 - 2026-08-15

## 요약

- **모델 저장소 자기완결화**: TTS 폴더(`D:\TTS`)만 복사하면 다른 PC에서도 바로 동작하도록 HF 모델 캐시를 TTS 내부로 이전
- 기존 공용 캐시(`D:\AI\models\unsloth`)와의 공유 제거, 구형/경량 모델은 저장공간 절약을 위해 미설치

## 진행 사항

### 모델 캐시 자기완결화 (config.py)

- [x] `backend/config.py`: `VOICEBOX_MODELS_DIR` 미설정 시 HF 캐시를 `data/models/hf-cache`로 기본 설정하도록 변경
- [x] TTS 내부 캐시에 필수 모델 설치/이전:
  - `Qwen/Qwen3-TTS-12Hz-1.7B-Base` (4.3G) — 음성 클로닝용
  - `Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice` (4.3G) — 프리셋 목소리
  - `Qwen/Qwen3-1.7B` (3.8G) — 받아쓰기 정제/LLM
  - `Qwen/Qwen3-TTS-Tokenizer-12Hz` (651M) — 의존성
  - `openai/whisper-large-v3` (2.9G) — 받아쓰기(STT). safetensors만 설치 (모든 포맷 설치 시 29G→2.9G 절약)
- [x] `Supertone/supertonic-3`는 기존대로 `data/models/supertonic-3`에 유지
- [x] TTS 내부 캐시 총 17G, TTS 폴더 전체 18G

### 정리

- [x] 공용 캐시(`D:\AI\models\unsloth`)에서 TTS 모델 3개를 TTS 내부로 이동
- [x] C: 캐시(`C:\Users\joo1\.cache\huggingface\hub`)의 중복 CustomVoice/Tokenizer 삭제 (8.1G→3.9G)
- [x] 중단된 0.6B-Base 다운로드 잔재 삭제
- [x] 외부 경로 참조(D:\AI, C:\캐시) 없음을 확인 — TTS 폴더만 복사하면 동작

### 미설치 (구형/경량, 저장공간 절약)

- `Qwen3-TTS-12Hz-0.6B-Base` / `0.6B-CustomVoice` — 1.7B의 구형 경량판
- `YatharthS/LuxTTS` — 1.7B로 대체 가능
- `openai/whisper-large-v3-turbo` — whisper-large-v3 구형
- `Qwen/Qwen3-0.6B` — 1.7B LLM의 구형 경량판

### 검증

- [x] `is_model_cached`로 필수 모델 4종 감지 확인 (Base/CustomVoice/Qwen3-1.7B/whisper 모두 True, 미설치 LuxTTS는 False)
- [x] `HF_HUB_CACHE = D:\TTS\data\models\hf-cache` 정상 설정

### 커밋

- `59f0281` feat: 모델 캐시를 TTS 폴더 내부(hf-cache)로 기본 설정
- `ff4b4d8` refactor: 미설치 구형 모델(LuxTTS, 0.6B, Whisper-Turbo) 메뉴 및 코드에서 제거

## 구형 모델 메뉴 제거

**배경**: 미설치 구형/경량 모델(LuxTTS, 0.6B 시리즈, Whisper-Turbo, Qwen3-0.6B)이 메뉴에 남아 있어 설치되지 않은 상태로 표시되어 혼동을 유발.

**제거된 모델**: `qwen-tts-0.6B`, `qwen-custom-voice-0.6B`, `luxtts`, `whisper-turbo`, `qwen3-0.6b`

**변경 사항**:
- 백엔드 `backends/__init__.py`: config 레지스트리에서 5개 구형 모델 제거 → 메뉴/모델 관리 UI에서 자동 제거
- 백엔드 검증 패턴(`models.py`): engine/STT/LLM 패턴에서 구형 값 제거, 기본값 `large`/`1.7B`로 통일
- LuxTTS 백엔드(`luxtts_backend.py`) 삭제, ZipVoice/linacodec 의존성 및 PyInstaller 설정 제거
- 프론트엔드: 엔진/모델 셀렉터, LLM/STT 설정 UI, 모델 관리에서 구형 옵션 제거
- 테스트: E2E 매트릭스, 다운로드/샘플 테스트에서 구형 모델 참조 정리
- i18n: 미사용 키(`turbo`, `size06`, `veryFast`) 9개 로케일에서 제거

**검증**: 전체 백엔드 임포트 OK, `py_compile` 전체 통과, `tsc --noEmit` 통과

## 남은 이슈 / 후속 작업

- git identity를 로컬로 설정 (`mmcafeju <snsmmcafe@gmail.com>`) — 기존 커밋 작성자와 동일
- 필요 시 구형/경량 모델 추가 설치 가능 (`data/models/hf-cache`에 snapshot_download로 바로 설치)